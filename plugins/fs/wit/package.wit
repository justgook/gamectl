package gams:fs@1.1.0;

interface fs {
    record dir-entry {
        name: string,
        %type: string,
    }

    record file-stat {
        %type: string,
        size: u64,
    }

    read-file: func(path: string) -> result<list<u8>, string>;
    read-text: func(path: string) -> result<string, string>;
    write-file: func(path: string, data: list<u8>) -> result<_, string>;
    write-text: func(path: string, text: string) -> result<_, string>;
    %list: func(path: string) -> result<list<dir-entry>, string>;
    stat: func(path: string) -> result<file-stat, string>;
    create-dir: func(path: string) -> result<_, string>;
    remove-file: func(path: string) -> result<_, string>;
    remove-dir: func(path: string) -> result<_, string>;
    rename: func(source: string, target: string) -> result<_, string>;
}

world fs-proxy {
    import wasi:filesystem/types@0.2.0;
    import wasi:filesystem/preopens@0.2.0;

    export fs;
}
