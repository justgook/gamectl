# Game runner reference

Resurrected from git history for reference.

Source commit: `fbe69c8c176eedae372b992f5000d2873ec45779^` (the parent of `🚧(browser): drop legacy version`).

Files:
- `view-game-runner.js` — legacy browser game runner view.
- `gl-bridge.js` — WebGL shim/bridge module used by the view.
- `024_game_runner_asset_sources.sql` — related asset-source migration used by the view.

The files are kept as historical references, so imports still point at their original `cmd/browser` layout.
